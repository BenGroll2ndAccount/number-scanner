import 'package:flutter/material.dart';

class SKU_HOMESCREEN extends StatelessWidget {
  const SKU_HOMESCREEN({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Enter SKU"),),
    );
  }
}