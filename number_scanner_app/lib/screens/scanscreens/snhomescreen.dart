import 'package:flutter/material.dart';

class SN_HOMESCREEN extends StatelessWidget {
  const SN_HOMESCREEN({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Enter SN"),),
    );
  }
}